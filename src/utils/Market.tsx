import { ReactNode } from "react";

export default function MarketDataProvider({
  children,
}: {
  children: ReactNode;
}) {
  // TODO: Implement provider of this data
  // TODO: implelment a useReducer() for fake data management

  return <>{children}</>;
}
